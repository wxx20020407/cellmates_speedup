# Helpers

This directory contains helpers that might be useful in multiple places (e.g. for VICTree project).
If needed, they can be moved to a separate submodule later.
Currently, it contains:

- `cnasim_data.py` script that converts CNASim data to an anndata object